pipeline {
    agent any

    stages {
        stage('Clone repository') {
            steps {
                git 'https://github.com/<username>/<repository-name>.git'
            }
        } 

        }
    }
